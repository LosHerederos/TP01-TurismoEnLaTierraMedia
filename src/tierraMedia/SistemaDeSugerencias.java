package tierraMedia;

import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class SistemaDeSugerencias {

	String mensaje = "A la Tierra Media!";

	public SistemaDeSugerencias() {
		
		
		System.out.println(mensaje);
		
		/*
		 * // Pruebas de clase Archivos
		 * System.out.println(Arrays.toString(Archivos.cargarUsuarios()));
		 * System.out.println(Arrays.toString(Archivos.cargarAtracciones()));
		 * Promocion[] promociones;
		 * promociones = Archivos.cargarPromociones(Archivos.cargarAtracciones());
		 * System.out.println(Arrays.toString(promociones)); try {
		 * Archivos.generarArchivosDeSalida(Archivos.cargarUsuarios()); } catch
		 * (IOException e) { // TODO Auto-generated catch block e.printStackTrace(); }
		 */
		
		/*
		 * // Pruebas del metodo toString
		 * Usuario usuario = new Usuario("Pedro", 100, 10.0,
		 * TipoDeAtracciones.AVENTURA); System.out.println(usuario); Atraccion atraccion
		 * = new Atraccion(25, 7.5, 10, TipoDeAtracciones.AVENTURA,
		 * "La torre del ojo de saruman"); System.out.println(atraccion);
		 * List<Atraccion> atracciones = new ArrayList<Atraccion>();
		 * atracciones.add(atraccion); Promocion promocion = new
		 * PromocionPorcentual("Pack Varios", atracciones, 20.0);
		 * System.out.println(promocion);
		 */
	}
	
	
}
